Extract ONLY the existing Pomi logo from the CENTER column B — Terminal of this comparison sheet. The user selected the TERMINAL logo, not A's apple monogram. Preserve the center column's exact forest-green rounded > prompt with its detached horizontal underscore and the green leaf rising behind its top edge, including the leaf's white vein. Preserve the existing rounded near-black lowercase 'pomi' wordmark underneath. Asset extraction, NOT redesign: keep shapes, proportions, green color, typography, and stacked arrangement as closely as possible. Output just the large center-column colored terminal/leaf icon and its pomi wordmark, centered with modest padding on an OPAQUE PURE WHITE background. Remove the B — Terminal heading, the small monochrome icon, separators, all other columns. No apple monogram, no mascot, no new elements, no glow or shadows.
